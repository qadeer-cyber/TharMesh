<?xml version="1.0" encoding="utf-8"?>
<!--
  Stage 6.1 — circular `+` chip at the end of the filter chip row. Mirrors
  the WhatsApp-style "add filter" affordance from the screenshot. Themeable
  fill so it sits cleanly on both light and dark backgrounds.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="?attr/tmBg2" />
    <stroke android:width="1dp" android:color="?attr/tmStroke" />
</shape>
