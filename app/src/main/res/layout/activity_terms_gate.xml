<?xml version="1.0" encoding="utf-8"?>
<!--
    Stage 8.3 — first-launch Terms of Use acceptance gate. The user
    must scroll through the body and tap "Accept and continue" before
    they can reach MainActivity / OnboardingActivity. "Decline and
    exit" finishes the app.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/tmBg"
    android:orientation="vertical">

    <TextView
        android:id="@+id/terms_gate_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:textColor="?attr/tmTextPrimary"
        android:textSize="22sp"
        android:textStyle="bold"
        android:text="@string/legal_terms_gate_title" />

    <TextView
        android:id="@+id/terms_gate_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="12dp"
        android:textColor="?attr/tmTextSecondary"
        android:textSize="14sp"
        android:text="@string/legal_terms_gate_subtitle" />

    <ScrollView
        android:id="@+id/terms_gate_scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:paddingStart="20dp"
        android:paddingEnd="20dp">

        <TextView
            android:id="@+id/terms_gate_body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="?attr/tmTextPrimary"
            android:textSize="15sp"
            android:lineSpacingMultiplier="1.25"
            android:text="@string/legal_doc_terms_body" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <Button
            android:id="@+id/terms_gate_accept"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/legal_terms_gate_accept" />

        <Button
            android:id="@+id/terms_gate_decline"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@android:color/transparent"
            android:textColor="?attr/tmTextSecondary"
            android:text="@string/legal_terms_gate_decline" />
    </LinearLayout>
</LinearLayout>
