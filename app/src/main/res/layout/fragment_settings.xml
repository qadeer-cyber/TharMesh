<?xml version="1.0" encoding="utf-8"?>
<!--
    WhatsApp-style settings tab. Shows a compact profile preview at the top
    that opens [ProfileActivity] on tap, followed by the same flat section
    list rendered by [SettingsSections]. The legacy card-list layout is
    replaced wholesale; the bottom-nav Settings tab and the chats top-bar
    avatar tap now lead to consistent UI surfaces.
-->
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/tmBg"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            style="@style/Text.H1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="12dp"
            android:text="@string/settings_title" />

        <!-- Profile preview row — taps to open ProfileActivity for the full
             editable header. Flat row, no card, matches the WhatsApp style. -->
        <LinearLayout
            android:id="@+id/settings_profile_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackground"
            android:gravity="center_vertical"
            android:minHeight="76dp"
            android:orientation="horizontal"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp">

            <FrameLayout
                android:layout_width="56dp"
                android:layout_height="56dp">

                <TextView
                    android:id="@+id/settings_avatar"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/bg_avatar"
                    android:gravity="center"
                    android:text="A"
                    android:textColor="@android:color/white"
                    android:textSize="22sp"
                    android:textStyle="bold" />

                <ImageView
                    android:id="@+id/settings_avatar_image"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:visibility="gone"
                    android:contentDescription="@string/profile_avatar_cd" />
            </FrameLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/settings_username"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="?attr/tmTextPrimary"
                    android:textSize="17sp"
                    android:textStyle="bold"
                    android:text="@string/profile_anonymous" />

                <TextView
                    android:id="@+id/settings_status"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:textColor="?attr/tmTextSecondary"
                    android:textSize="13sp"
                    android:text="@string/profile_status_placeholder" />
            </LinearLayout>

            <ImageView
                android:layout_width="@dimen/tm_icon"
                android:layout_height="@dimen/tm_icon"
                android:src="@drawable/ic_chevron_right"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                app:tint="?attr/tmTextSecondary" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:background="?attr/tmStrokeSoft" />

        <LinearLayout
            android:id="@+id/settings_rows"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="24dp" />
    </LinearLayout>
</androidx.core.widget.NestedScrollView>
