<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen_gradient"
    android:orientation="vertical"
    android:fitsSystemWindows="true">

    <!--
      Stage 6.3 — persistent Disaster Mode banner. Sits above the mesh-status
      banner so the user always sees it regardless of permission / scanning
      state. Drawable is a solid red; tinted via theme attrs so light + dark
      both render as urgent red without redesigning either palette.
    -->
    <LinearLayout
        android:id="@+id/disaster_mode_banner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:visibility="gone"
        android:background="?attr/tmDanger">

        <TextView
            android:id="@+id/disaster_mode_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="@string/disaster_banner_active"
            xmlns:tools="http://schemas.android.com/tools"
            tools:text="Disaster Mode active — battery low" />
    </LinearLayout>

    <!--
      Mesh status banner removed in the Profile/UI polish pass — the same
      signal is now surfaced as a small warning dot on the mesh icon in the
      Chats top bar (tap → enable-Bluetooth dialog). Keeps the chrome clean.
    -->

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!--
      Stage 6.1 — WhatsApp-style bottom nav. itemBackground paints a soft
      green pill behind the active tab (state_checked=true, see
      drawable/bg_nav_pill.xml); inactive tabs render flat. The pill is
      themed so it appears as the WhatsApp green tint in light mode and the
      neon-green-dim glow in dark mode, preserving the pre-6.1 cyber look.

      Stage 9.2 — wrapped in a floating glass MaterialCardView so the nav
      bar reads as "lifted above" the screen surface (depth Layer 3). The
      legacy 1dp top divider is removed — the glass card's stroke handles
      the surface separation now.
    -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="10dp"
        app:cardBackgroundColor="?attr/tmSurfaceGlassFloating"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        app:strokeColor="?attr/tmStrokeSoft"
        app:strokeWidth="1dp">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_nav"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:minHeight="64dp"
            app:itemBackground="@drawable/bg_nav_pill"
            app:itemIconTint="@color/nav_item_color"
            app:itemTextColor="@color/nav_item_color"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_nav" />
    </com.google.android.material.card.MaterialCardView>

</LinearLayout>
