<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card_glass"
    android:foreground="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:layout_marginBottom="@dimen/tm_space_s"
    android:minHeight="@dimen/tm_row_settings_min_height"
    android:orientation="horizontal"
    android:padding="@dimen/tm_space_l">

    <FrameLayout
        android:id="@+id/row_icon_bg"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_round_icon_cyan">

        <ImageView
            android:id="@+id/row_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_user" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/row_title"
            style="@style/Text.Body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:text="Profile" />

        <TextView
            android:id="@+id/row_subtitle"
            style="@style/Text.Caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Name, avatar, user ID" />
    </LinearLayout>

    <ImageView
        android:layout_width="@dimen/tm_icon"
        android:layout_height="@dimen/tm_icon"
        android:src="@drawable/ic_chevron_right" />
</LinearLayout>
